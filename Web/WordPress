WordPress Web Structure consists of PHP and MySQL.
We could use some command to enumerate the information.

1.Enumeration: wpscan --url {The URL of Target} {OPTIONS} .

1-1.-e (= --enumeration):It's a parameter for enumerating. If the value is u, it would
enumerate the user information.If the value is p, it would enumerate the plugin inforamtion.
Ex:wpscan --url http://192.x.x.x -e u,p

1-2.Some Another wpscan command:
.wpscan --url http://sandbox.local -e u,t,ap,cb,dbe
.wpscan --url http://192.x.x.x -e p --plugins-detection aggressive

1-3.In some situation, wpscan need token api, so we could infer below command:
wpscan --url http://192.x.x.x --api-token {Random token} 
Moreover, raandom token could be retrived from below:
https://wpscan.com/api/

1-4.After enumerating, we could find the vulnerability for below section:(Important!!!!!!!)
Core、Theme、Plugin

2.Path Enumeration:

2.1.gobuster
2.2.feroxbuster

If you got below wordpress path, you could do further thing to find the vulnerability:
.wp-login.php
.wp-admin
.phpmyadmin.php
.robots.txt
.sitemap.xml

3.curl Enumeration
